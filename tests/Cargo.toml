[package]
name = "shielded-pool-tests"
version = "0.1.0"
edition = "2021"

[dependencies]
shielded-pool-lib = { path = "../lib" }
hex = { workspace = true }
anyhow = { workspace = true }
serde_json = { workspace = true }

[dev-dependencies]
rand = "0.8"
