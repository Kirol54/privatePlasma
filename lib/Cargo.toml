[package]
name = "shielded-pool-lib"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1.0", default-features = false, features = ["derive", "alloc"] }
tiny-keccak = { workspace = true }
